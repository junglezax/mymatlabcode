11:38 2014-10-07
初步向量化
向量化以后居然效果好很多：优化达到了最大步数，没报below那个警告
画出的图形包含一些可分辨的图案，而不是一片雪花
难道和非向量化的代码不等价？原来的代码有错？
...是采样数太少的原因？采样为10000，似乎也正常，400步走完，给出光滑一点的图案。看来不是这个原因
采样数10000，大小16*16，隐单元数（特征数）36，用时，204s
改用原来的非向量化代码，用进167s，但是42步就出现Step Size below TolX，画出的图案只是一些花纹
采样数10000，大小8*8，隐单元数（特征数）25，用时44s

MINST，train-images.idx3-ubyte，10000个数字，用时1672s，得到的图案和教程上基本一致

问题：
分割，采样再提取特征有啥道理？
为何不整体提取？

分割采样后，识别时怎么做？
提取到的特征filters，作用到整幅图像上（是否要作卷积？），得到一些压缩的特征，再用logistic/softmax来分类？

